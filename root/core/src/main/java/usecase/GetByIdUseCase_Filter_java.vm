#if ( !$entity.name.contains("Enum_") )
#parse ( "include/domain_info.vm" )
$generator.generate($target.entityName, "Get${entity.name}By${domainId}UseCase.java", $target.folder, "root/core/src/main/java/usecase/GetByIdUseCase_java.vm" )
#end